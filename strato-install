1. install docker compose
 https://developers.blockapps.net/install/linux/linux/ 

2. restart ./strato.sh --wipe (clear data , will delete data from blockchain)
	 

3. strato stop ./strato.sh --stop (stop a running instance of strato)


Configure PBFT

$ ./strato.sh keygen --3

In strato-getting-started run ./strato.sh --keygen --{count}, 
where count is the number of private key and address pairs to generate: