
// var request = require('request');

// var user = "test"
// var password = "test"
// // curl -X POST "http://localhost/bloc/v2.2/users/test" -H "accept: application/json;charset=utf-8" -H "Content-Type: application/x-www-form-urlencoded" -d "password=test"

// request.post('http://localhost/bloc/v2.2/users/'+user, {form:{password:password}}, function (error, response, body) {
//   console.log('body:', body); 
// //   console.log('response', response); 
// })

//this will create new user with the name and address will got in response 
